#!/usr/bin/env bash

echo "plugin_with-all-valid-CHARS-123 plugin documentation"
echo
echo "plugin_with-all-valid-CHARS-123 plugin is a plugin only used for unit tests"

if [ -n "$ASDF_INSTALL_VERSION" ]; then
  echo
  echo "Details specific for version $ASDF_INSTALL_VERSION"
fi
